import 'package:flutter/material.dart';

class paginaInicio extends StatelessWidget {
  const paginaInicio ({key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Thrift3r')),

    );
  }
}